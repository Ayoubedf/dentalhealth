"use strict";var openBtns=document.querySelectorAll("[data-open-modal]"),closeBtn=document.querySelector("[data-close-modal]"),modal=document.querySelector("[data-modal]"),services=modal.querySelectorAll("#dialog-service > option");function selectService(selectService){services.forEach(function(service){service.value===selectService?service.selected=selectService:service.removeAttribute("selected")})}openBtns.forEach(function(openBtn){openBtn.onclick=function(){modal.showModal(),selectService(openBtn.dataset.openModal)}}),closeBtn.onclick=function(){modal.close(),selectService("")};